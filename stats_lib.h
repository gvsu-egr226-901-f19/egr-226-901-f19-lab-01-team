#include <stdio.h>
#include <stdlib.h>

float maximum(float nums[], int n);         //custom function to find the maximum value
float minimum (float nums[], int n);         //custom function to find the minimum value
float mean(float nums[], int n);         //custom function to find average value
float median(float nums[], int n);         //custom function to find value in the middle of the ascending number set
float variance(float nums[], int n);         //custom function to find the variance
float standard_deviation(float nums[], int n);         //custom function to find standard deviation
